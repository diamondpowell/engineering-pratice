public boolean sleepIn(boolean weekday, boolean vacation) 
{
  // weekday = is today a weekday?
  // vacation = are we on vacation?
  
  // if !weekday || vacation
    // return true
  // if weekday || !vacation
    // return false
  //if weekday || vacation
    // return true
  
  if (!weekday || vacation)
  {
    return true;
  }
  else
  {
    return false;
  }
}
